# Chapter: 3. Getting started with Kargo
# Section: Defining the Warehouse
# Kargo version: 1.x
# Status: UNVERIFIED

apiVersion: kargo.akuity.io/v1alpha1
kind: Warehouse
metadata:
  name: demo-app
  namespace: demo
spec:
  subscriptions:
  - image:
      repoURL: ghcr.io/kargobook/demo-app
      semverConstraint: ">=1.0.0 <2.0.0"

