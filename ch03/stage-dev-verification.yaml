# Chapter: 3. Getting started with Kargo
# Section: 3.2 The core resource model
# Kargo version: 1.11.0
# Status: UNVERIFIED
#
# ILLUSTRATIVE - DO NOT APPLY. Fragment showing only the `verification` block
# a Stage gains, and where it sits among the fields the dev Stage already has.
# Merge it into your full Stage manifest; there is no apiVersion/kind here, so
# `kubectl apply` has nothing to act on. (Same convention as
# ch04/warehouse-git-subscription.yaml and the other subscription fragments.)
#
# This file previously carried `apiVersion`/`kind`/`metadata.name: dev`, plus a
# `promotionTemplate.spec` whose only content was a comment. That parses as
# null, `steps` is `omitempty`, and the CRD accepts it, so applying it would
# have stripped the promotion steps off the live `dev` Stage and every later
# Promotion would have been rejected for having no steps.

# tag::content[]
spec:
  requestedFreight:
  - origin:
      kind: Warehouse
      name: demo-app
    sources:
      direct: true
  verification:
    analysisTemplates:
    - name: dev-health                       # <1>
  promotionTemplate:
    spec:
      # ... steps unchanged from the dev Stage above
# end::content[]
