# Chapter: 7. Multi-Service and Multi-Team Patterns
# Section: Shared Warehouses
# Kargo version: 1.x
# Status: UNVERIFIED

# Managed by the platform team in the kargo namespace
apiVersion: kargo.akuity.io/v1alpha1
kind: Warehouse
metadata:
  name: base-images
  namespace: kargo
spec:
  subscriptions:
  - image:
      repoURL: ghcr.io/example/base-image
      constraint: ">=3.0.0 <4.0.0"
  - image:
      repoURL: ghcr.io/example/sidecar-proxy
      constraint: ">=1.5.0 <2.0.0"

